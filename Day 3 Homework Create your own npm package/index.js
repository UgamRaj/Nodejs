const add = (a, b) => a + b;
const subTract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;

const calc = {
  add,
  subTract,
  multiply,
  divide,
};

module.exports = calc;
